# scripts/approval/
> L2 | 父级: ../AGENTS.md

成员清单

approval_gate.py: `thirtyx.approval` 的 create/verify 兼容 CLI wrapper。

live execution 的单一授权规则位于 `thirtyx.approval`；此目录不得复制实现。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
